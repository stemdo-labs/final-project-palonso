apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-multi-deployment.fullname" . }}
  labels:
    {{- include "my-multi-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-multi-deployment.selectorLabels" . | nindent 4 }}
